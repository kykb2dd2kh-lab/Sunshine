# ☀️ 阳光计划 - 部署到手机主屏幕指南

## 方法一：GitHub Pages 部署（推荐，永久免费）

### 第1步：注册/登录 GitHub
- 打开 [github.com](https://github.com)，注册或登录账号

### 第2步：创建新仓库
1. 点击右上角 **`+`** → **New repository**
2. 仓库名填：`sunshine-plan`
3. 选择 **Public**（公开）
4. 勾选 **Add a README file**
5. 点击 **Create repository**

### 第3步：上传项目文件
1. 在仓库页面点击 **`Add file`** → **Upload files**
2. 将 `sunshine-plan.zip` 解压后，把里面所有文件拖拽上传
   - `index.html`、`style.css`、`data.js`、`app.js`
   - `manifest.json`、`.nojekyll`
   - `icon-192.png`、`icon-512.png`、`apple-touch-icon.png`、`favicon-32.png`
3. 点击 **Commit changes**

### 第4步：开启 GitHub Pages
1. 进入仓库 → **Settings** → 左侧 **Pages**
2. Source 选择 **Deploy from a branch**
3. Branch 选 **main** → 文件夹选 **`/ (root)`**
4. 点击 **Save**

### 第5步：获取网址
等待1-2分钟后刷新，页面顶部会出现：
```
https://你的用户名.github.io/sunshine-plan/
```

### 第6步：添加到手机主屏幕

#### iPhone（Safari 打开网址）
1. 用 **Safari** 打开上面的网址
2. 点击底部 **分享按钮 ⬆️**
3. 选择 **"添加到主屏幕"**
4. 点击 **"添加"**
5. 桌面出现太阳图标，点开就是全屏App

#### Android（Chrome 打开网址）
1. 用 **Chrome** 打开网址
2. 页面会自动弹出 **"添加到主屏幕"** 按钮
3. 或点浏览器菜单 `⋮` → **"添加到主屏幕"**

---

## 方法二：本地直接使用（无需部署）

如果不想部署，也可以直接在手机上用：
1. 把项目文件传到手机
2. 用手机浏览器打开 `index.html`
3. 同样可以"添加到主屏幕"

> 注意：本地方式换手机后数据不互通

---

## ✅ 功能特性

| 特性 | 说明 |
|------|------|
| 📱 全屏运行 | 像App一样，不显示地址栏 |
| 🎨 独立图标 | 太阳笑脸图标显示在桌面 |
| 🔒 本地存储 | 数据存在手机本地，保护隐私 |
| 📲 竖屏锁定 | 适合手机竖屏使用 |
| 🌱 离线可用 | 首次加载后，断网也能打卡 |

---

## ❓ 常见问题

**Q: 换手机后数据还在吗？**
A: 数据存在手机浏览器本地，换手机不会同步。如需同步，需要后续接入云存储。

**Q: iPhone 必须用 Safari 吗？**
A: 是的，只有 Safari 支持"添加到主屏幕"的PWA功能。Chrome for iOS 不支持。

**Q: 部署后网址打不开？**
A: GitHub Pages 首次部署需要等1-2分钟，刷新页面即可。
